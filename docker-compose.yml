services:
  api:
    build:
      context: ./backend
    env_file:
      - ./backend/.env
    ports:
      - "4000:4000"
    restart: unless-stopped
    volumes:
      - api_uploads:/app/uploads

volumes:
  api_uploads:

